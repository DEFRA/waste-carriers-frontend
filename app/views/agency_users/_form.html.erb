<%= form_for(@agency_user) do |f| %>
  <% if @agency_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agency_user.errors.count, "error") %> prohibited this agency_user from being saved:</h2>

      <ul>
      <% @agency_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p class="group email">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </p>
  <p class="group field">
    <%= f.label :password %>
    <%= f.password_field :password %>
  </p>
  <p class="action group">
    <input name="next" class="button" type="submit" value="<%if @agency_user.persisted?%>Update<%else%>Create<%end%> agency user"/>
  </p>
<% end %>