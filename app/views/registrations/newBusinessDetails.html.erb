<%= render 'content_for_pages' %>

<main id="content" role="main" class="group">
  <article role="article" class="group">

    <% content_for :page_title, createTitle('registrations.businessdetails_step.title') %>
    <%= form_for(@registration, :url => newBusinessDetails_path) do |f| %>

      <div class="step"><%= t 'registrations.businessdetails_step.step' %></div>
      <h1 class="subtitle"><%= t 'registrations.businessdetails_step.subtitle' %></h1>

      <%= render "businessdetails_step", :f => f %>

    <% end %>

  </article>
</main>
