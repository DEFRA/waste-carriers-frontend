<%= render 'shared/content_for_pages' %>

<div id="wrapper">

  <main id="content" role="main" class="group">

    <div class="back-section">
      <ol>
        <li><%= render 'back_link' %></li>
      </ol>
    </div>

			<% content_for :page_title, createTitle('registrations.registrationtype_step.title') %>
			<%= form_for(@registration, :url => newRegistrationType_path, html: {autocomplete: 'off'}) do |f| %>
  			<%= render "registrationtype_step", :f => f %>
		  <% end %>

  </main><!--main end-->
</div><!--wrapper end-->