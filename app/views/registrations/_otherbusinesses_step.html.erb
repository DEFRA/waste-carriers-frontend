<%= render "errors" %>

<!-- WF 4.3.1 -->
<div id="page2" data-journey="waste-carriers-registration:otherbusinesses">

  <!-- Smart Answers
    Whose waste do you deal with?
   -->
  <fieldset>
    <p class="group<%if @registration.errors[:otherBusinesses].any?%> validation<%end%>">
      <span class="inline"><!-- GDS have an 'option' class but we prefer this simplified version -->
        <%= f.radio_button :otherBusinesses, "yes", :class => "radio" %> <%= f.label t 'form.yes_option_label', :class => "label" %>
      </span>
  	  <span class="inline">
  	    <%= f.radio_button :otherBusinesses, "no", :class => "radio" %> <%= f.label t 'form.no_option_label', :class => "label" %>
  	  </span>
    </p>
  </fieldset>

</div>

<%= render 'wizard_links' %>
