<%= render 'shared/content_for_pages' %>

<div id="wrapper">

  <main id="content" role="main" class="group">

    <div class="back-section">
      <ol>
        <li><%= render 'back_link' %></li>
      </ol>
    </div>

  	<section>
  		<% content_for :page_title, createTitle('registrations.serviceprovided_step.title') %>
  		<%= form_for(@registration, :url => newServiceProvided_path, html: {autocomplete: 'off'}) do |f| %>
  		<%= render "serviceprovided_step", :f => f %>
		  <% end %>
	  </section>

  </main><!--main end-->
</div><!--wrapper end-->