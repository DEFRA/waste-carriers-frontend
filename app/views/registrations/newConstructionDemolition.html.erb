<%= render 'shared/content_for_pages' %>

<div id="wrapper">

  <main id="content" role="main" class="group">

    <div class="back-section">
      <ol>
        <li><%= render 'back_link' %></li>
      </ol>
    </div>

    		<% content_for :page_title, createTitle('registrations.constructiondemolition_step.title') %>
    		<%= form_for(@registration, :url => newConstructionDemolition_path, html: {autocomplete: 'off'}) do |f| %>
      		<%= render "constructiondemolition_step", :f => f %>
    		<% end %>

  </main><!--main end-->
</div><!--wrapper end-->
