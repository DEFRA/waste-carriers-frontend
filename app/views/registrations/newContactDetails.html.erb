<%= render 'shared/content_for_pages' %>

<div id="wrapper">

  <main id="content" role="main" class="group">

    <div class="back-section">
      <ol>
        <li><%= render 'back_link' %></li>
      </ol>
    </div>

      <!-- WF 4.3.2 -->
      <div id="page7"  data-journey="waste-carriers-registration:contact-details">
        <% content_for :page_title, createTitle('.title') %>

          <%= form_for(@registration, :url => newContact_path, html: {autocomplete: 'off'}) do |f| %>
          <%= render 'contactdetails_step', f: f, r: @registration %>
          <%= render 'next_link' %>
        <% end %>
      </div>

  </main><!-- end main -->
</div><!-- end wrapper -->
