<main id="content" role="main" class="group">
  <article role="article" class="group">

    <% content_for :page_title, createTitle('registrations.contactdetails_step.title') %>
    <%= form_for(@registration, :url => newContact_path) do |f| %>

      <div class="step"><%= t 'registrations.contactdetails_step.step' %></div>
      <h1 class="subtitle"><%= t 'registrations.contactdetails_step.subtitle' %></h1>

      <%= render "contactdetails_step", f: f, registration_phase: 'lower' %>

    <% end %>

  </article>
</main>
