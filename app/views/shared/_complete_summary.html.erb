<%# 
This file expects a parameter 'type' with the following values:
almostComplete    - renders appropriate text, with orange background
complete          - renders appropriate text, with green background
complete lower    - renders appropriate text, with green background
criminallySuspect - renders appropriate text, with orange background
Note: These values should match styles in the css if needed
%>


<% if RegistrationsHelper::COMPLETE_STATUSES.include? type %>
<!-- Show a summary box of the status of the registration -->
<div class="completeSummary <%= type %>">
	<div class="completeSummaryTitle">
		<%= t'.almost_there_title' if type.eql? RegistrationsHelper::STATUS_ALMOST_COMPLETE %>
		<%= t'.criminal_title' if type.eql? RegistrationsHelper::STATUS_CRIMINALLY_SUSPECT %>
		<%= t'.complete_title' if type.eql? RegistrationsHelper::STATUS_COMPLETE %>
		<%= t'.complete_title' if type.eql? RegistrationsHelper::STATUS_COMPLETE_LOWER %>
	</div>
	<div class="completeSummaryMessage">
		<%= t '.almost_there_message' if type.eql? RegistrationsHelper::STATUS_ALMOST_COMPLETE %>
		<%= t'.criminal_message' if type.eql? RegistrationsHelper::STATUS_CRIMINALLY_SUSPECT %>
		<%= t'.complete_message' if type.eql? RegistrationsHelper::STATUS_COMPLETE %>
	</div>
</div>
<% else %>
<!-- Error: Invalid type provided, should be one of almostComplete, complete, criminallySuspect -->
<% end %>