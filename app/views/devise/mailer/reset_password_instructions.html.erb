<h2><%= t 'devise.mailer.reset_password_instructions.heading'%></h2>

<p><%= t 'devise.mailer.reset_password_instructions.paragraph1'%></p>

<p><%= link_to t('devise.mailer.reset_password_instructions.changeLabel'), edit_password_url(@resource, :reset_password_token => @token, :subdomain => @resource.subdomain) %></p>

<p><%= t 'devise.mailer.reset_password_instructions.paragraph2'%></p>
<p><%= t 'devise.mailer.reset_password_instructions.paragraph3'%></p>
